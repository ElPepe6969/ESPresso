# Credentials file — NEVER COMMIT
# Copy this to sdkconfig.credentials and fill in your values.
# Or use: pio run -t menuconfig → "MicroLink V2 Configuration → Credentials"

CONFIG_ML_WIFI_SSID=""
CONFIG_ML_WIFI_PASSWORD=""
CONFIG_ML_TAILSCALE_AUTH_KEY=""
CONFIG_ML_DEVICE_NAME="esp32-wol"
